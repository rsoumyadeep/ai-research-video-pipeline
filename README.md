# AI Research Video Pipeline

An automated pipeline that:

* tracks trending research papers
* generates summaries and scripts
* converts them into audio/video
* uploads to YouTube

## Structure

* `src/` → core pipeline modules
* `config/` → topics and settings
* `data/` → generated outputs
* `assets/` → media assets

## Status

🚧 Initial skeleton created
